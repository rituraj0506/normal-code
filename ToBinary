#include<bits/stdc++.h>
using namespace std;

void tobinary(int n)
{
 vector<int>ans;
   while(n!=0)
   {
    ans.push_back(n%2);
    n=n/2;
   }
   reverse(ans.begin(),ans.end());
   for(auto k:ans)
   cout<<k<<" ";
}
int main()
{
    int n;
    cin>>n;
    tobinary(n);
}
